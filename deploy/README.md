# Archivos de despliegue

```text
deploy/
├── aws/          API Express en EC2, systemd, Nginx y variables de ejemplo
├── hostinger/    frontend Angular estático y fallback SPA
└── frontend-vm/  alternativa académica si el frontend se monta en otra VM
```

Flujo principal: [Hostinger + AWS](../DESPLIEGUE_HOSTINGER_AWS.md).
